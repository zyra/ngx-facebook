machine:
  node:
    version: 5.5.0

general:
  branches:
    ignore:
      - gh-pages

test:
  override:
    - npm run build
    - node scripts/update_docs.js

deployment:
 staging:
   branch: v2
   commands:
     - bash ./scripts/update_docs.sh
