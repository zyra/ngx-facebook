machine:
  node:
    version: 5.5.0

general:
  branches:
    only:
      - v2

test:
  override:
    - npm run build
    - node scripts/update_docs.js

deployment:
 staging:
   branch: v2
   commands:
     - bash ./scripts/update_docs.sh
